Dependencies:
To run the code the following libraries are needed:
Matplotlib
Pandas
Numpy

They can be installed by:
pip install matplotlib
pip install pandas
pip install numpy

A possible error could occur where for questions 4 to 6, the code cannot find the csv file to read. Download and add them in the same directory to fix this.

All python files can normally be run
